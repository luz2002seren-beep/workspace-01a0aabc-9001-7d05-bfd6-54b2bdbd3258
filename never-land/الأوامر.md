# 📜 قائمة أوامر Never Land الكاملة

> **27 أمر سلاش** موزّعة على **4 أقسام** + **١٠ أنظمة** جاهزة.
> اكتب `/` في ديسكورد ثم اسم الأمر. للاطلاع التفاعلي: `/help`.

| القسم | العدد | الأوامر |
|---|---|---|
| 🤖 **عام** | 6 | `/avatar` `/botinfo` `/help` `/ping` `/serverinfo` `/userinfo` |
| 🔨 **الإشراف** | 9 | `/ban` `/kick` `/lock` `/member` `/purge` `/slowmode` `/timeout` `/unban` `/warn` |
| ⚙️ **الإعدادات** | 10 | `/autoline` `/automod` `/autoreact` `/autorole` `/leveling` `/logs` `/settings` `/setup` `/tickets` `/welcome` |
| 🛠️ **أدوات** | 2 | `/poll` `/remind` |

---

## 🤖 عام (6)
*أوامر عامة ومعلومات*

### `/avatar`
> عرض صورة العضو أو البانر الخاص به

*الكولداون: 3 ثانية • الملف: `src/commands/general/avatar.js`*

   - `العضو` (عضو، اختياري) — العضو المطلوب
   - `النوع` (نص، اختياري) — ما تريد عرضه — القيم: `الصورة (Avatar)` / `البانر (Banner)` / `صورة السيرفر (Icon)`

---

### `/botinfo`
> معلومات عن البوت وإحصائياته

*الكولداون: 5 ثانية • الملف: `src/commands/general/botinfo.js`*

---

### `/help`
> عرض قائمة أوامر البوت والمساعدة

*الكولداون: 2 ثانية • الملف: `src/commands/general/help.js`*

   - `الأمر` (نص، اختياري) — اسم أمر معيّن لعرض تفاصيله

---

### `/ping`
> قياس سرعة استجابة البوت وجودة الاتصال

*الكولداون: 5 ثانية • الملف: `src/commands/general/ping.js`*

---

### `/serverinfo`
> معلومات تفصيلية عن السيرفر

*الكولداون: 5 ثانية • الملف: `src/commands/general/serverinfo.js`*

---

### `/userinfo`
> معلومات تفصيلية عن عضو (الحساب، الرتب، السجل، المستوى)

*الكولداون: 3 ثانية • الملف: `src/commands/general/userinfo.js`*

   - `العضو` (عضو، اختياري) — العضو المطلوب


---

## 🔨 الإشراف (9)
*حظر، طرد، إسكات، تحذيرات، تنظيف*

### `/ban`
> حظر عضو من السيرفر (مع دعم الحظر المؤقت)

*الصلاحية: **حظر الأعضاء** • صلاحيات البوت: حظر الأعضاء • الكولداون: 3 ثانية • الملف: `src/commands/moderation/ban.js`*

   - `العضو` (عضو، **إلزامي**) — العضو المطلوب حظره
   - `السبب` (نص، اختياري) — سبب الحظر
   - `المدة` (نص، اختياري) — مدة اختيارية مثل 7d أو 2h (اتركها فارغة لحظر دائم)
   - `حذف_الرسائل` (عدد، اختياري) — حذف رسائله خلال آخر X يوم (0-7)

---

### `/kick`
> طرد عضو من السيرفر

*الصلاحية: **طرد الأعضاء** • صلاحيات البوت: طرد الأعضاء • الكولداون: 3 ثانية • الملف: `src/commands/moderation/kick.js`*

   - `العضو` (عضو، **إلزامي**) — العضو المطلوب طرده
   - `السبب` (نص، اختياري) — سبب الطرد

---

### `/lock`
> قفل أو فتح قناة (منع الأعضاء من الكتابة)

*الصلاحية: **إدارة القنوات** • صلاحيات البوت: إدارة القنوات • الكولداون: 3 ثانية • الملف: `src/commands/moderation/lock.js`*

**`/lock channel`** — قفل قناة
   - `القناة` (قناة، اختياري) — القناة
   - `السبب` (نص، اختياري) — السبب

**`/lock unlock`** — فتح قناة
   - `القناة` (قناة، اختياري) — القناة

**`/lock all`** — قفل كل القنوات النصية (حالة طوارئ)
   - `السبب` (نص، اختياري) — السبب

**`/lock unlockall`** — فتح كل القنوات النصية

---

### `/member`
> إدارة الأعضاء: الاسم المستعار، الرتب، الحالات، التصفير

*الصلاحية: **إدارة الأسماء المستعارة** • الكولداون: 3 ثانية • الملف: `src/commands/moderation/member.js`*

**`/member nickname`** — تغيير الاسم المستعار لعضو
   - `العضو` (عضو، **إلزامي**) — العضو
   - `الاسم` (نص، اختياري) — الاسم الجديد (اتركه فارغًا لإزالة الاسم المستعار)

**`/member role`** — إعطاء أو سحب رتبة من عضو
   - `العضو` (عضو، **إلزامي**) — العضو
   - `الرتبة` (رتبة، **إلزامي**) — الرتبة

**`/member history`** — سجل العقوبات لعضو
   - `العضو` (عضو، **إلزامي**) — العضو

**`/member reset`** — تصفير كل العقوبات المسجّلة لعضو
   - `العضو` (عضو، **إلزامي**) — العضو

---

### `/purge`
> حذف رسائل بشكل جماعي (تنظيف القناة)

*الصلاحية: **إدارة الرسائل** • صلاحيات البوت: إدارة الرسائل، ReadMessageHistory • الكولداون: 5 ثانية • الملف: `src/commands/moderation/purge.js`*

**`/purge messages`** — حذف عدد من الرسائل
   - `العدد` (عدد، **إلزامي**) — عدد الرسائل (1-100)
   - `العضو` (عضو، اختياري) — حذف رسائل عضو معيّن فقط

**`/purge contains`** — حذف الرسائل التي تحتوي على نص معيّن
   - `النص` (نص، **إلزامي**) — النص المطلوب البحث عنه
   - `العدد` (عدد، **إلزامي**) — أقصى عدد رسائل للبحث (1-100)

**`/purge links`** — حذف الرسائل التي تحتوي روابط
   - `العدد` (عدد، **إلزامي**) — أقصى عدد رسائل للبحث (1-100)

**`/purge bot`** — حذف رسائل البوتات
   - `العدد` (عدد، **إلزامي**) — أقصى عدد رسائل للبحث (1-100)

**`/purge user`** — حذف كل رسائل عضو في القناة
   - `العضو` (عضو، **إلزامي**) — العضو
   - `العدد` (عدد، **إلزامي**) — أقصى عدد رسائل للبحث (1-100)

---

### `/slowmode`
> ضبط الوضع البطيء للقناة

*الصلاحية: **إدارة القنوات** • صلاحيات البوت: إدارة القنوات • الكولداون: 3 ثانية • الملف: `src/commands/moderation/slowmode.js`*

   - `المدة` (نص، **إلزامي**) — مثل 5s أو 1m — اكتب 0 لإلغاء الوضع البطيء
   - `القناة` (قناة، اختياري) — القناة (افتراضيًا القناة الحالية)

---

### `/timeout`
> إسكات عضو مؤقتًا (Timeout) أو فك الإسكات

*الصلاحية: **إسكات الأعضاء (Timeout)** • صلاحيات البوت: إسكات الأعضاء (Timeout) • الكولداون: 3 ثانية • الملف: `src/commands/moderation/timeout.js`*

**`/timeout add`** — تطبيق إسكات مؤقت
   - `العضو` (عضو، **إلزامي**) — العضو
   - `المدة` (نص، **إلزامي**) — المدة مثل 10m أو 2h (أقصى 28 يوم)
   - `السبب` (نص، اختياري) — السبب

**`/timeout remove`** — فك الإسكات عن عضو
   - `العضو` (عضو، **إلزامي**) — العضو
   - `السبب` (نص، اختياري) — السبب

**`/timeout list`** — عرض الأعضاء المُسكتين حاليًا

---

### `/unban`
> فك الحظر عن عضو باستخدام الآيدي أو اسم المستخدم

*الصلاحية: **حظر الأعضاء** • صلاحيات البوت: حظر الأعضاء • الكولداون: 3 ثانية • الملف: `src/commands/moderation/unban.js`*

   - `العضو` (نص، **إلزامي**) — آيدي العضو أو اسمه الكامل (مثال: name#0000)
   - `السبب` (نص، اختياري) — سبب فك الحظر

---

### `/warn`
> نظام التحذيرات: إضافة، عرض، حذف

*الصلاحية: **إسكات الأعضاء (Timeout)** • الكولداون: 3 ثانية • الملف: `src/commands/moderation/warn.js`*

**`/warn add`** — تحذير عضو
   - `العضو` (عضو، **إلزامي**) — العضو
   - `السبب` (نص، اختياري) — سبب التحذير
   - `خاص` (منطقي، اختياري) — إرسال التحذير في الخاص فقط (بدون إعلان)

**`/warn list`** — عرض تحذيرات عضو
   - `العضو` (عضو، **إلزامي**) — العضو

**`/warn remove`** — حذف تحذير برقمه
   - `الرقم` (عدد، **إلزامي**) — رقم التحذير (Case ID)

**`/warn clear`** — حذف كل تحذيرات عضو
   - `العضو` (عضو، **إلزامي**) — العضو


---

## ⚙️ الإعدادات (10)
*إعداد كل أنظمة البوت*

### `/autoline`
> الخط الفاصل التلقائي: يرسل خطًا بعد كل رسالة في قنوات محدّدة 

*الصلاحية: **إدارة السيرفر** • الكولداون: 3 ثانية • الملف: `src/commands/config/autoline.js`*

**`/autoline enable`** — تفعيل الخط الفاصل التلقائي

**`/autoline disable`** — تعطيل الخط الفاصل التلقائي

**`/autoline add`** — إضافة قناة يعمل فيها الخط الفاصل
   - `القناة` (قناة، **إلزامي**) — القناة

**`/autoline remove`** — إزالة قناة من الخط الفاصل
   - `القناة` (قناة، **إلزامي**) — القناة

**`/autoline line`** — تغيير شكل الخط
   - `النص` (نص، **إلزامي**) — نص الخط (يدعم إيموجيات خارجية)

**`/autoline color`** — تلوين الخط (ليس له تأثير إلا في الـ embed)
   - `اللون` (نص، **إلزامي**) — كود اللون مثل #5865f2 أو اكتب remove للحذف

**`/autoline options`** — خيارات متقدّمة: حذف الخط السابق / الحذف التلقائي
   - `حذف_الخط_السابق` (منطقي، اختياري) — حذف الخط السابق عند وصول رسالة جديدة
   - `حذف_بعد_ثواني` (عدد، اختياري) — حذف الخط تلقائيًا بعد كم ثانية (0 = بدون)
   - `حذف_مع_الرسالة` (منطقي، اختياري) — حذف الخط عند حذف رسالته

**`/autoline test`** — إرسال خط تجريبي في القناة الحالية

**`/autoline status`** — عرض إعدادات الخط الفاصل

---

### `/automod`
> إعداد الحماية التلقائية (مضاد السبام، الروابط، الكلمات الممنوعة...)

*الصلاحية: **إدارة السيرفر** • الكولداون: 3 ثانية • الملف: `src/commands/config/automod.js`*

**`/automod enable`** — تفعيل الحماية التلقائية

**`/automod disable`** — تعطيل الحماية التلقائية

**`/automod toggle`** — تفعيل أو تعطيل حماية معيّنة
   - `الحماية` (نص، **إلزامي**) — نوع الحماية — القيم: `مضاد السبام` / `مضاد الروابط` / `مضاد دعوات السيرفرات` / `مضاد منشن الجميع` / `مضاد المنشن الجماعي` / `مضاد الحروف الكبيرة`

**`/automod punishment`** — تحديد العقوبة عند رصد مخالفة
   - `العقوبة` (نص، **إلزامي**) — العقوبة المطبّقة — القيم: `حذف الرسالة فقط` / `تحذير` / `إسكات مؤقت` / `طرد` / `حظر`

**`/automod limits`** — ضبط حدود السبام
   - `عدد_الرسائل` (عدد، اختياري) — عدد الرسائل المسموح به
   - `خلال_ثواني` (عدد، اختياري) — خلال كم ثانية
   - `مدة_الإسكات` (عدد، اختياري) — مدة الإسكات بالدقائق عند اختيار timeout

**`/automod word`** — إدارة الكلمات الممنوعة (إضافة/حذف)
   - `عملية` (نص، **إلزامي**) — إضافة أو حذف — القيم: `إضافة` / `حذف`
   - `الكلمة` (نص، **إلزامي**) — الكلمة المطلوبة

**`/automod words`** — عرض كل الكلمات الممنوعة

**`/automod whitelist`** — استثناء قناة أو رتبة من الحماية
   - `القناة` (قناة، اختياري) — قناة مستثناة
   - `الرتبة` (رتبة، اختياري) — رتبة مستثناة

**`/automod status`** — عرض إعدادات الحماية الحالية

---

### `/autoreact`
> التفاعلات التلقائية: إيموجيات تُضاف تلقائيًا لرسائل قنوات محدّدة

*الصلاحية: **إدارة السيرفر** • الكولداون: 3 ثانية • الملف: `src/commands/config/autoreact.js`*

**`/autoreact enable`** — تفعيل التفاعلات التلقائية

**`/autoreact disable`** — تعطيل التفاعلات التلقائية

**`/autoreact channel`** — إضافة/إزالة قناة للتفاعل التلقائي
   - `القناة` (قناة، **إلزامي**) — القناة

**`/autoreact emoji`** — إضافة إيموجي للتفاعل التلقائي (يدعم الإيموجيات الخارجية)
   - `العملية` (نص، **إلزامي**) — إضافة أو حذف — القيم: `إضافة` / `حذف`
   - `الإيموجي` (نص، **إلزامي**) — مثال: 👍 أو <:name:123456789012345678>

**`/autoreact word`** — تفاعل عند احتواء الرسالة على كلمة معيّنة
   - `العملية` (نص، **إلزامي**) — إضافة أو حذف — القيم: `إضافة` / `حذف`
   - `الكلمة` (نص، **إلزامي**) — الكلمة المفتاحية
   - `الإيموجي` (نص، اختياري) — الإيموجي للتفاعل

**`/autoreact test`** — إرسال رسالة تجريبية ورؤية التفاعلات

**`/autoreact status`** — عرض الإعدادات الحالية

---

### `/autorole`
> الرتب التلقائية للأعضاء الجدد

*الصلاحية: **إدارة السيرفر** • الكولداون: 3 ثانية • الملف: `src/commands/config/autorole.js`*

**`/autorole add`** — إضافة رتبة تُمنح للأعضاء الجدد
   - `الرتبة` (رتبة، **إلزامي**) — الرتبة
   - `للبوتات` (منطقي، اختياري) — تُمنح للبوتات فقط؟

**`/autorole remove`** — حذف رتبة من قائمة الرتب التلقائية
   - `الرتبة` (رتبة، **إلزامي**) — الرتبة

**`/autorole list`** — عرض الرتب التلقائية الحالية

**`/autorole enable`** — تفعيل الرتب التلقائية

**`/autorole disable`** — تعطيل الرتب التلقائية

---

### `/leveling`
> نظام المستويات: التفعيل، الترتيب، لوحة المتصدرين، المكافآت

*الكولداون: 3 ثانية • الملف: `src/commands/config/leveling.js`*

**`/leveling rank`** — عرض رتبتك أو رتبة عضو
   - `العضو` (عضو، اختياري) — العضو

**`/leveling leaderboard`** — لوحة المتصدرين في السيرفر
   - `الصفحة` (عدد، اختياري) — رقم الصفحة

**`/leveling enable`** — تفعيل نظام المستويات (يحتاج صلاحية الإدارة)
   - `قناة_الإعلان` (قناة، اختياري) — قناة إعلانات الترقية

**`/leveling disable`** — تعطيل نظام المستويات

**`/leveling config`** — ضبط الخبرة والكولداون
   - `أدنى_خبرة` (عدد، اختياري) — أدنى خبرة لكل رسالة
   - `أعلى_خبرة` (عدد، اختياري) — أعلى خبرة لكل رسالة
   - `الكولداون` (عدد، اختياري) — ثواني بين كل خبرة وأخرى
   - `خبرة_صوتية` (منطقي، اختياري) — منح خبرة على البقاء في القنوات الصوتية

**`/leveling reward`** — إدارة مكافآت الرتب عند الوصول لمستوى معيّن
   - `عملية` (نص، **إلزامي**) — إضافة أو حذف — القيم: `إضافة` / `حذف`
   - `المستوى` (عدد، **إلزامي**) — المستوى المطلوب
   - `الرتبة` (رتبة، اختياري) — الرتبة الممنوحة

**`/leveling rewards`** — عرض كل مكافآت الرتب

**`/leveling reset`** — تصفير خبرة عضو (يحتاج إدارة)
   - `العضو` (عضو، **إلزامي**) — العضو

---

### `/logs`
> السجلات: قناة اللوقات واختيار الأحداث (38 حدثًا في 9 مجموعات)

*الصلاحية: **إدارة السيرفر** • الكولداون: 3 ثانية • الملف: `src/commands/config/logs.js`*

**`/logs channel`** — تحديد قناة السجلات
   - `القناة` (قناة، اختياري) — القناة (اتركها فارغة للمعاينة فقط)

**`/logs enable`** — تفعيل نظام السجلات

**`/logs disable`** — تعطيل نظام السجلات

**`/logs events`** — اختيار الأحداث المسجّلة حسب المجموعة (قوائم تفاعلية)

**`/logs all`** — تفعيل كل الأحداث (38)

**`/logs none`** — تعطيل كل الأحداث

**`/logs ignore`** — استثناء قناة أو رتبة من السجلات
   - `القناة` (قناة، اختياري) — قناة مستثناة
   - `الرتبة` (رتبة، اختياري) — رتبة مستثناة

**`/logs status`** — عرض حالة السجلات والأحداث المفعّلة

**`/logs test`** — إرسال رسالة تجريبية إلى قناة السجلات

---

### `/settings`
> إعدادات البوت العامة: عرض الحالة، اللغة، لوحة التحكم، إعادة الضبط

*الصلاحية: **إدارة السيرفر** • الكولداون: 3 ثانية • الملف: `src/commands/config/settings.js`*

**`/settings status`** — عرض كل إعدادات البوت في هذا السيرفر

**`/settings language`** — تغيير لغة ردود البوت
   - `اللغة` (نص، **إلزامي**) — اختر اللغة — القيم: `العربية` / `English`

**`/settings dashboard`** — رابط لوحة التحكم وطريقة الدخول

**`/settings reset`** — إعادة كل إعدادات البوت في السيرفر إلى الافتراضي
   - `تأكيد` (منطقي، **إلزامي**) — اكتب نعم للتأكيد

---

### `/setup`
> معالج إعداد البوت خطوة بخطوة (ترحيب، لوقات، حماية، تذاكر...)

*الصلاحية: **إدارة السيرفر** • الكولداون: 3 ثانية • الملف: `src/commands/config/setup.js`*

---

### `/tickets`
> إدارة نظام التذاكر (الدعم الفني • التوثيق • الهدايا • تقديم إدارة)

*الصلاحية: **إدارة السيرفر** • الكولداون: 3 ثانية • الملف: `src/commands/config/tickets.js`*

**`/tickets setup`** — إعداد التذاكر: القسم، رتب الدعم، قناة السجلات
   - `القسم` (قناة، **إلزامي**) — قسم (Category) تُنشأ فيه التذاكر
   - `رتبة_الدعم_1` (رتبة، اختياري) — رتبة فريق الدعم
   - `رتبة_الدعم_2` (رتبة، اختياري) — رتبة دعم إضافية
   - `قناة_السجلات` (قناة، اختياري) — قناة تُرسل فيها أرشيف التذاكر

**`/tickets panel`** — إرسال لوحة البنل مع رسالة التعليمات في قناة
   - `القناة` (قناة، اختياري) — القناة (افتراضيًا الحالية)

**`/tickets panelmode`** — وضع اللوحة: أزرار (Buttons) أو قائمة منسدلة (Select)
   - `الوضع` (نص، **إلزامي**) — اختر شكل اللوحة — القيم: `🔘 أزرار` / `📋 قائمة منسدلة`

**`/tickets panelinfo`** — تعديل رسالة التعليمات والعنوان والوصف للوحة
   - `رسالة_التعليمات` (نص، اختياري) — نص الرسالة المرفقة مع البنل
   - `العنوان` (نص، اختياري) — عنوان البنل
   - `الوصف` (نص، اختياري) — وصف البنل

**`/tickets applychannel`** — 📝 تحديد قناة مراجعة طلبات تقديم الإدارة
   - `القناة` (قناة، **إلزامي**) — القناة التي تُرسل فيها الطلبات

**`/tickets applycategory`** — 📁 القسم الذي تُنقل إليه تذاكر المقبولين (تكتات الإدارة)
   - `القسم` (قناة، **إلزامي**) — قسم تكتات الإدارة

**`/tickets applyrole`** — 🎭 رتبة تُعطى تلقائيًا لمن يُقبل تقديمه
   - `الرتبة` (رتبة، **إلزامي**) — رتبة الإدارة
   - `إزالة` (منطقي، اختياري) — إزالة الرتبة من قائمة القبول بدل إضافتها

**`/tickets applyping`** — 🔔 رتبة تُنبَّه عند وصول طلب تقديم جديد
   - `الرتبة` (رتبة، **إلزامي**) — رتبة الإدارة

**`/tickets applytoggle`** — 📝 تفعيل/تعطيل استمارة تقديم الإدارة
   - `التفعيل` (منطقي، **إلزامي**) — true = تفعيل • false = تعطيل

**`/tickets application`** — 📝 عرض إعدادات استمارة تقديم الإدارة

**`/tickets enable`** — تفعيل نظام التذاكر

**`/tickets disable`** — تعطيل نظام التذاكر

**`/tickets list`** — عرض كل التذاكر (مفتوحة ومغلقة)
   - `الحالة` (نص، اختياري) — تصفية — القيم: `مفتوحة` / `مغلقة`

**`/tickets addtype`** — إضافة نوع تذكرة جديد (يدعم إيموجيات خارجية)
   - `الآيدي` (نص، **إلزامي**) — مُعرّف بالإنجليزية مثل temp
   - `الاسم` (نص، **إلزامي**) — الاسم الظاهر بالعربية
   - `إيموجي` (نص، اختياري) — إيموجي عادي أو خارجي <:name:id>
   - `وصف` (نص، اختياري) — وصف قصير يظهر في اللوحة
   - `رسالة_داخلية` (نص، اختياري) — نص تعليمات يظهر داخل التذكرة

**`/tickets removetype`** — حذف نوع تذكرة
   - `الآيدي` (نص، **إلزامي**) — مُعرّف النوع

**`/tickets types`** — عرض الأنواع الحالية + خيار استرجاع الأنواع الأربعة الافتراضية
   - `الافتراضية` (منطقي، اختياري) — استرجاع الأنواع الأربعة الافتراضية (دعم فني، توثيق، هدايا، تقديم إدارة)

**`/tickets add`** — إضافة عضو إلى التذكرة الحالية
   - `العضو` (عضو، **إلزامي**) — العضو

**`/tickets stats`** — إحصائيات التذاكر

---

### `/welcome`
> إعداد رسائل الترحيب والوداع والدعم + صورة الترحيب

*الصلاحية: **إدارة السيرفر** • الكولداون: 3 ثانية • الملف: `src/commands/config/welcome.js`*

**`/welcome channel`** — تحديد قناة الترحيب
   - `القناة` (قناة، **إلزامي**) — القناة

**`/welcome message`** — تعديل نص رسالة الترحيب
   - `النص` (نص، **إلزامي**) — نص الرسالة مع المتغيّرات
   - `embed` (منطقي، اختياري) — إرسالها كـ embed أنيق؟

**`/welcome image`** — صورة الترحيب: بطاقة بالأفتار / أفتار / صورة مخصّصة / بدون
   - `النوع` (نص، **إلزامي**) — نوع الصورة — القيم: `🖼️ بطاقة مولَّدة بالأفتار (Card)` / `👤 صورة الأفتار فقط` / `🔗 رابط صورة مخصّصة` / `🚫 بدون صورة`
   - `الرابط` (نص، اختياري) — رابط الصورة (عند اختيار صورة مخصّصة)
   - `خلفية` (نص، اختياري) — رابط خلفية للبطاقة (اختياري)
   - `صورة_مرفقة` (منطقي، اختياري) — إرسال الصورة كمرفق (أفضل جودة)

**`/welcome enable`** — تفعيل الترحيب

**`/welcome disable`** — تعطيل الترحيب

**`/welcome dm`** — تفعيل/تعطيل رسالة ترحيب في الخاص
   - `الحالة` (منطقي، **إلزامي**) — تفعيل؟

**`/welcome test`** — إرسال ترحيب تجريبي (مع الصورة) لتراه بنفسك
   - `العضو` (عضو، اختياري) — العضو التجريبي

**`/welcome leave`** — إعداد رسالة الوداع
   - `القناة` (قناة، **إلزامي**) — قناة الوداع
   - `النص` (نص، اختياري) — نص رسالة الوداع

**`/welcome boost`** — إعداد رسالة شكر الدعم (Boost)
   - `القناة` (قناة، **إلزامي**) — قناة الدعم
   - `النص` (نص، اختياري) — نص الرسالة

**`/welcome status`** — عرض إعدادات الترحيب الحالية


---

## 🛠️ أدوات (2)
*تذكيرات وتصويت*

### `/poll`
> إنشاء تصويت سريع في القناة

*الكولداون: 5 ثانية • الملف: `src/commands/utility/poll.js`*

   - `السؤال` (نص، **إلزامي**) — سؤال التصويت
   - `خيار1` (نص، **إلزامي**) — الخيار الأول
   - `خيار2` (نص، **إلزامي**) — الخيار الثاني
   - `خيار3` (نص، اختياري) — الخيار الثالث
   - `خيار4` (نص، اختياري) — الخيار الرابع
   - `خيار5` (نص، اختياري) — الخيار الخامس

---

### `/remind`
> ضبط تذكير وسيُنبّهك البوت في الوقت المحدّد

*الكولداون: 5 ثانية • الملف: `src/commands/utility/remind.js`*

   - `المدة` (نص، **إلزامي**) — بعد كم من الوقت؟ مثل 10m أو 2h أو 3d
   - `النص` (نص، **إلزامي**) — ماذا أذكّرك به؟


---

## 📝 نظام تقديم الإدارة (Application)

```
العضو يفتح تذكرة "تقديم إدارة"
        ↓
البوت يرسل رسالة فيها زر: 📝 املأ نموذج التقديم
        ↓
نموذج (Modal) من 5 خانات:
   1) الاسم والعمر        2) من أي بلد أنت؟
   3) سيرفرات كنت فيها إدارياً   4) هل تقدر تفتح مايك؟
   5) هل تقدر تحط شعار ورابط؟
        ↓
الإجابات تروح لقناة مراجعة الإدارة + زرّين: ✅ قبول | ❌ رفض
        ↓
❌ رفض ➜ يكتب المشرف السبب ➜ رسالة خاصة للمتقدّم: تم رفض عرضك + السبب
✅ قبول ➜ رسالة خاصة: تم قبول عرضك
          + داخل التذكرة: تم قبول عرضك
          + إعادة تسمية التذكرة باسمه وترقيمها (إدارة-001-ahmed)
          + نقل التذكرة لقسم تكتات الإدارة + إعطاؤه الرتبة
```

| الأمر | الوظيفة |
|---|---|
| `/tickets applychannel` | تحديد قناة مراجعة الطلبات |
| `/tickets applycategory` | قسم تكتات الإدارة للمقبولين |
| `/tickets applyrole` | رتبة تُعطى عند القبول (وأزرار الإضافة/الإزالة) |
| `/tickets applyping` | رتبة تُنبَّه عند وصول طلب |
| `/tickets applytoggle` | تفعيل/تعطيل الاستمارة |
| `/tickets application` | عرض الإعدادات الحالية + الخانات الخمس |

---

## 📜 أحداث السجلات (38 حدثًا في 9 مجموعات)

| المجموعة | الأحداث |
|---|---|
| 💬 **الرسائل** | حذف رسالة • تعديل رسالة • حذف جماعي للرسائل |
| 👥 **الأعضاء** | دخول عضو • خروج عضو • طرد عضو • حظر عضو • فك حظر • إسكات عضو (Timeout) • دعم السيرفر (Boost) • تغيير رتب عضو • تغيير اسم عضو • تغيير صورة عضو • تغيير اسم المستخدم |
| 🎭 **الرتب** | إنشاء رتبة • حذف رتبة • تعديل رتبة |
| 📁 **القنوات** | إنشاء قناة • حذف قناة • تعديل قناة • تغيير صلاحيات قناة |
| ⚙️ **السيرفر والإيموجيات** | تحديث السيرفر • إضافة إيموجي • حذف إيموجي • إضافة ملصق • حذف ملصق |
| 🔗 **الدعوات** | إنشاء دعوة • حذف دعوة |
| 🧵 **المواضيع** | إنشاء موضوع (Thread) • حذف موضوع (Thread) |
| 🔊 **الصوت** | دخول قناة صوتية • خروج من قناة صوتية • تنقّل بين قنوات صوتية • كتم صوتي • إصمام صوتي |
| 🛡️ **الأنظمة** | إجراء إداري • الحماية التلقائية • التذاكر |

> تُفعّل/تُعطّل من اللوحة (تبويب السجلات) أو من ديسكورد: `/logs events` ثم اختر المجموعة.

---

## 💡 أمثلة سريعة

```
/ban العضو:@Ahmed السبب:نشر روابط المدة:7d حذف_الرسائل:3
/timeout add العضو:@Ahmed المدة:30m السبب:سبام
/automod word عملية:إضافة الكلمة:كلمة_ممنوعة
/autoline add القناة:#التقديمات
/autoreact word العملية:إضافة الكلمة:مبروك الإيموجي:🎉
/welcome image النوع:بطاقة مولَّدة بالأفتار
/tickets setup القسم:الدعم رتبة_الدعم_1:@Support
/tickets applychannel القناة:#قبول-ادارة    ← قناة مراجعة التقديمات
/tickets applycategory القسم:تكتات الادارة
/tickets applyrole الرتبة:@الإدارة
/tickets panelmode الوضع:أزرار
/tickets types الافتراضية:نعم
/logs events
```

> 🎫 أوامر الإعداد تحتاج صلاحية **إدارة السيرفر**، وأوامر الإشراف تحتاج الصلاحية المناسبة.
> 🌐 كل إعداد موجود في الأوامر يمكن تعديله أيضًا من لوحة التحكم الويب (تبويب 📝 تقديم الإدارة).