# 🤖 Never Land | بوت إدارة سيرفرات ديسكورد متكامل + لوحة تحكم

بوت ديسكورد احترافي مبني بـ **Node.js + discord.js v14**، يجمع كل ميزات إدارة السيرفرات في مشروع واحد مفتوح المصدر،
مع **لوحة تحكم ويب عربية بالكامل** تُدار من المتصفح.

> **الموقع الرسمي:** هذا المشروع يشغّل موقعًا حقيقيًا (صفحات مُولَّدة من الخادم + واجهة API + قاعدة بيانات)،
> وليس نسخة HTML ثابتة: `/` الصفحة الرئيسية، `/dashboard` لوحة السيرفرات، `/dashboard/:id` لوحة الإعداد الكاملة،
> مع `/icon.svg` و`/manifest.webmanifest` و`/robots.txt` و`/sitemap.xml` وصفحة 404 مصمّمة.
> الاسم الظاهر في كل الصفحات يُضبط من متغيّر `SITE_NAME` (الافتراضي: **Never Land**).

> ✨ **الميزة الأقوى:** العربية أولًا. كل ردود البوت ولوحة التحكم بالعربية (مع دعم الإنجليزية)،
> وفلتر الكلمات الممنوعة يتعامل مع الكتابة العربية الشائعة (تشكيل، همزات، تاء مربوطة...).

---

## 📋 المحتويات

- [الميزات](#-الميزات)
- [البدء السريع](#-البدء-السريع)
- [إعداد ديسكورد (Developer Portal)](#-إعداد-ديسكورد-developer-portal)
- [إعداد لوحة التحكم (OAuth2)](#-إعداد-لوحة-التحكم-oauth2)
- [أوامر البوت](#-أوامر-البوت)
- [لوحة التحكم](#-لوحة-التحكم)
- [الإعدادات الكاملة](#-الإعدادات-الكاملة)
- [هيكل المشروع](#-هيكل-المشروع)
- [قاعدة البيانات](#-قاعدة-البيانات)
- [النشر على سيرفر](#-النشر-على-سيرفر)
- [استكشاف الأخطاء](#-استكشاف-الأخطاء)

---

## 🚀 الميزات

### 🛡️ الحماية التلقائية (AutoModeration)
| الميزة | الوصف |
|---|---|
| مضاد السبام | يرصد الإرسال السريع المتكرر (عدد رسائل / فترة زمنية) ويحذف/يعاقب |
| مضاد الروابط | يحذف الروابط مع قائمة دومينات مسموحة (تنسور، جيفي...) |
| مضاد دعوات السيرفرات | يمنع `discord.gg/...` وكل صيغ الدعوات |
| مضاد منشن الجميع | يمنع `@everyone` و `@here` |
| مضاد المنشن الجماعي | يمنع الإشارة لعدد كبير من الأعضاء |
| مضاد الحروف الكبيرة | يمنع الرسائل المكتوبة بحروف كبيرة (نسبة قابلة للضبط) |
| الكلمات الممنوعة | فلتر ذكي يفهم العربية: `أ/ا`، `ة/ه`، `ى/ي`، التشكيل والتطويل |
| مكافحة الهجمات | Anti-Raid: يرصد الانضمام الجماعي المشبوه ويمنع الحسابات الجديدة |
| قوائم الاستثناء | استثناء قنوات أو رتب محدّدة |
| العقوبات | حذف / تحذير / إسكات مؤقت / طرد / حظر |

### 🔨 أوامر الإشراف
حظر دائم ومؤقت (يُفك تلقائيًا) • طرد • إسكات وفك إسكات • تحذيرات (إضافة/عرض/حذف/تصفير)
• تنظيف الرسائل (بعدد/عضو/محتوى/روابط/بوتات) • قفل وفتح القنوات (واحد أو الكل)
• الوضع البطيء • الاسم المستعار • إدارة الرتب • سجل حالات كامل لكل عضو.

### 📜 السجلات (Logging) — تغطية شاملة
**38 حدثًا في 9 مجموعات** يمكن تشغيل كل حدث على حدة:

| المجموعة | الأحداث |
|---|---|
| 💬 الرسائل | حذف • تعديل • حذف جماعي |
| 👥 الأعضاء | دخول • خروج • طرد • حظر • فك حظر • إسكات • دعم (Boost) • تغيير رتب • اسم • صورة • اسم مستخدم |
| 🎭 الرتب | إنشاء • حذف • تعديل |
| 📁 القنوات | إنشاء • حذف • تعديل • **تغيير الصلاحيات** |
| ⚙️ السيرفر | تحديث الإعدادات • إيموجيات (إضافة/حذف/تعديل) • ملصقات |
| 🔗 الدعوات | إنشاء • حذف |
| 🧵 المواضيع | إنشاء • حذف |
| 🔊 الصوت | دخول • خروج • تنقّل • كتم • إصمام |
| 🛡️ الأنظمة | إجراءات إدارية • حماية تلقائية • تذاكر |

**مع كشف من فعل ماذا** من سجل التدقيق (Audit Log) + استثناء قنوات/رتب من السجلات.

### ➖ الخط الفاصل التلقائي (AutoLine)
خط فاصل يُرسل تلقائيًا بعد كل رسالة في قنوات محدّدة — مثالي لقنوات التقديمات.
- شكل الخط قابل للتخصيص (يدعم الإيموجيات الخارجية)
- حذف الخط السابق تلقائيًا عند وصول رسالة جديدة
- حذف الخط مع حذف رسالته + حذف تلقائي بعد مدة

### 😀 التفاعلات التلقائية (AutoReaction)
- تفاعل تلقائي بإيموجيات محدّدة على كل رسالة في قنوات محدّدة
- **تفاعل عند احتواء الرسالة على كلمة مفتاحية** (ترحيب ➜ 👋)
- دعم كامل للإيموجيات الخارجية والمتحرّكة `<a:name:id>`

### 👋 الترحيب والتفاعل — مع صورة بالأفتار
- **صورة ترحيب بالبطاقة:** بطاقة مولَّدة بـ Canvas تحتوي أفتار العضو + اسمه + رقم انضمامه + اسم السيرفر
- أو صورة الأفتار مباشرة، أو صورة مخصّصة برابط، أو بدون صورة
- خلفية قابلة للتخصيص + تدرّج لوني للبطاقة
- متغيّرات: `{user}` `{username}` `{displayName}` `{server}` `{memberCount}` `{avatar}` `{createdAt}` `{joinedAt}`
- ترحيب في الخاص • وداع • شكر الدعم (Boost) • رتب تلقائية • تتبّع من دعا مَن

### 🎫 التذاكر — 4 أنواع جاهزة
**🛠️ الدعم الفني • ✅ التوثيق • 🎁 الهدايا • 📋 تقديم إدارة**

- **لوحة بنل** مع رسالة تعليمات منفصلة (يمكن تعديل نصّها)
- **وضعان للوحة:** أزرار (Buttons) أو قائمة منسدلة (Select)
- **دعم الإيموجيات الخارجية** في الأزرار والأنواع والرسائل `<:name:id>`
- لكل نوع: وصف، إيموجي، رسالة داخلية، ولون الزر — الكل قابل للتعديل من اللوحة أو من ديسكورد
- داخل التذكرة: 🙋 استلام • 🔒 إغلاق • 📄 أرشيف • ➕ إضافة عضو
- إغلاق مع **أرشيف محادثة كامل** يُرسل لقناة السجلات + رسالة خاصة لصاحب التذكرة

### 📝 نظام تقديم الإدارة (Applications) — بضغطة زر
تذكرة **"تقديم إدارة"** ما تبقى روم فاضي — البوت يشتغل بدالك:

1. **يفتح نموذج (Modal) من 5 خانات** — نفس اللي طلبته بالحرف:

| # | الخانة |
|---|---|
| 1 | الاسم والعمر |
| 2 | من أي بلد أنت؟ |
| 3 | سيرفرات كنت فيها إداريًا؟ |
| 4 | هل تقدر تفتح مايك؟ |
| 5 | هل تقدر تحط شعار ورابط؟ |

2. **الإجابات تروح مباشرة لقناة مراجعة الإدارة** مع أزرار: ✅ **قبول العرض** • ❌ **رفض العرض** • 🔗 الذهاب للتذكرة.
3. **❌ رفض** ➜ يكتب المشرف السبب في نموذج صغير ➜ يوصل للمتقدّم **رسالة خاصة**: «لقد تم رفض عرضك» + السبب.
4. **✅ قبول** ➜
   - **رسالة خاصة للمتقدّم**: «🎉 لقد تم قبول عرضك» + رقم ملفه
   - **رسالة داخل التذكرة**: «لقد تم قبول عرضك» مع تنبيهه
   - **إعادة تسمية التذكرة** باسمه وترقيمها: `إدارة-001-ahmed`
   - **نقلها إلى قسم تكتات الإدارة** تلقائيًا
   - **إعطاؤه الرتبة** المحدّدة (رتبة الإدارة)
5. كل الرسائل والخانات **قابلة للتعديل** من اللوحة (تبويب 📝 تقديم الإدارة) أو من ديسكورد.

| الأمر | الوظيفة |
|---|---|
| `/tickets applychannel` | قناة مراجعة الطلبات |
| `/tickets applycategory` | قسم تكتات الإدارة (مكان نقل المقبولين) |
| `/tickets applyrole` | رتبة تُعطى عند القبول |
| `/tickets applyping` | رتبة تُنبَّه عند وصول طلب جديد |
| `/tickets applytoggle` | تفعيل/تعطيل الاستمارة |
| `/tickets application` | عرض الإعدادات والخانات الحالية |

### 📈 المستويات
خبرة على الرسائل (منحنى عادل تصاعدي) • خبرة صوتية لكل دقيقة • مكافآت رتب تلقائية عند مستويات محدّدة
• `/leveling rank` و `/leveling leaderboard` • كولداون واستثناء قنوات/رتب.

### 🌐 لوحة تحكم ويب
تسجيل دخول بـ Discord (OAuth2) • تعديل كل الإعدادات من المتصفح • رسم بياني للنشاط
• جداول الحالات والتذاكر والمتصدرين • إغلاق التذاكر من اللوحة • واجهة عربية RTL داكنة بالكامل.

### ➕ إضافات
تذكيرات (`/remind`) • تصويت (`/poll`) • إعداد تفاعلي داخل ديسكورد بـ `/setup` • أمر `/help` بأزرار
• دعم لغتين • تشغيل/إيقاف كل شيء بأمر واحد.

---

## ⚡ البدء السريع

```bash
# 1) ثبّت الحزم
npm install

# 2) انسخ ملف الإعدادات واملأه
cp .env.example .env
#    عدّل: DISCORD_TOKEN و CLIENT_ID و CLIENT_SECRET و SESSION_SECRET

# 3) شغّل المشروع (البوت + لوحة التحكم)
npm start
```

| الأمر | الوظيفة |
|---|---|
| `npm start` | تشغيل البوت + لوحة التحكم معًا |
| `npm run bot` | البوت فقط (`--no-web`) |
| `npm run web` | لوحة التحكم فقط (`--no-bot`) |
| `npm run deploy` | تسجيل أوامر السلاش يدويًا |
| `npm test` | تشغيل اختبارات سريعة (الأوامر، الأحداث، قاعدة البيانات، الحماية) |

> 🧪 **تجربة اللوحة بدون بوت:** اضبط `DEMO_MODE=true` في `.env` ثم `npm run web`
> — ستفتح اللوحة ببيانات تجريبية كاملة بدون تسجيل دخول.

---

## 🔧 إعداد ديسكورد (Developer Portal)

1. افتح [Discord Developer Portal](https://discord.com/developers/applications) → **New Application**.
2. من **Bot**:
   - انسخ **Token** → ضعه في `DISCORD_TOKEN`.
   - فعّل الـ Intents التالية (إلزامية):
     - ✅ **SERVER MEMBERS INTENT**
     - ✅ **MESSAGE CONTENT INTENT**
   - انسخ **Application ID** → `CLIENT_ID`.
3. من **OAuth2 → General**: انسخ **Client Secret** → `CLIENT_SECRET`.
   وأضف **Redirect URL**: `http://localhost:3000/auth/callback`
   (أو `https://نطاقك.com/auth/callback` عند النشر).
4. **دعوة البوت:** OAuth2 → URL Generator:
   - Scopes: `bot` + `applications.commands`
   - Permissions الموصى بها: `Administrator` (أو الصلاحيات المطلوبة فقط:
     Manage Server, Ban, Kick, Moderate Members, Manage Messages, Manage Channels,
     Manage Roles, Manage Nicknames, View Audit Log, Send Messages, Embed Links, Attach Files, Read Message History)
5. **مهم:** اجعل رتبة البوت **أعلى** من الرتب التي سيديرها في السيرفر.

---

## 🌐 إعداد لوحة التحكم (OAuth2)

لاستخدام تسجيل الدخول الحقيقي:

1. تأكد من `CLIENT_ID` و `CLIENT_SECRET` في `.env`.
2. اضبط `DASHBOARD_URL` ليطابق رابط العودة **حرفيًا** (مثال: `http://localhost:3000`).
3. غيّر `SESSION_SECRET` إلى نص طويل عشوائي:
   ```bash
   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
   ```
4. شغّل `npm start` وافتح `http://localhost:3000`.

**من يستطيع الدخول؟** فقط من يملك صلاحية **إدارة السيرفر** أو **Admin** في سيرفر مُضاف فيه البوت.

> ⚠️ عند النشر على الإنترنت: شغّل اللوحة خلف **HTTPS** واجعل `cookie.secure = true`
> في `src/web/server.js`.

---

## 🎮 أوامر البوت

<details>
<summary><b>الإشراف (moderation)</b></summary>

| الأمر | الوصف |
|---|---|
| `/ban` | حظر عضو (يدعم مدة مؤقتة مثل `7d` وحذف رسائله) |
| `/unban` | فك الحظر بالآيدي أو الاسم |
| `/kick` | طرد عضو |
| `/timeout add\|remove\|list` | إسكات مؤقت / فك الإسكات / عرض المُسكتين |
| `/warn add\|list\|remove\|clear` | نظام التحذيرات الكامل |
| `/purge messages\|contains\|links\|bot\|user` | تنظيف القناة بخيارات متعددة |
| `/slowmode` | الوضع البطيء |
| `/lock channel\|unlock\|all\|unlockall` | قفل/فتح القنوات |
| `/member nickname\|role\|history\|reset` | إدارة عضو وسجله |
</details>

<details>
<summary><b>الإعدادات (config)</b></summary>

| الأمر | الوصف |
|---|---|
| `/setup` | معالج تفاعلي لإعداد البوت كاملًا |
| `/settings status\|language\|dashboard\|reset` | إعدادات عامة |
| `/logs channel\|enable\|disable\|events\|all\|none\|ignore\|status\|test` | السجلات (38 حدثًا) |
| `/welcome channel\|message\|image\|enable\|disable\|dm\|test\|leave\|boost\|status` | الترحيب + صورة الأفتار |
| `/automod enable\|disable\|toggle\|punishment\|limits\|word\|words\|whitelist\|status` | الحماية التلقائية |
| `/tickets setup\|panel\|panelmode\|panelinfo\|types\|enable\|disable\|list\|addtype\|removetype\|add\|stats` | التذاكر (4 أنواع) |
| `/tickets applychannel\|applycategory\|applyrole\|applyping\|applytoggle\|application` | 📝 استمارة تقديم الإدارة |
| `/leveling rank\|leaderboard\|enable\|disable\|config\|reward\|rewards\|reset` | المستويات |
| `/autorole add\|remove\|list\|enable\|disable` | الرتب التلقائية |
| `/autoline enable\|disable\|add\|remove\|line\|color\|options\|test\|status` | الخط الفاصل التلقائي |
| `/autoreact enable\|disable\|channel\|emoji\|word\|test\|status` | التفاعلات التلقائية |
</details>

<details>
<summary><b>عام + أدوات</b></summary>

| الأمر | الوصف |
|---|---|
| `/help` | قائمة الأوامر بأزرار تنقل + بحث عن أمر |
| `/ping` | سرعة الاستجابة ومدة التشغيل |
| `/botinfo` `/serverinfo` `/userinfo` `/avatar` | معلومات وإحصائيات |
| `/remind` | تذكير بعد مدة (`10m`, `2h`, `3d`) |
| `/poll` | تصويت سريع حتى 5 خيارات |
</details>

---

## 🖥️ لوحة التحكم — 13 قسمًا

اللوحة صارت شريطًا جانبيًا بدل التبويبات المكدّسة: **13 قسمًا في 6 مجموعات**، وكل مفتاح بجانب الإعداد تبعه.

| المجموعة | الأقسام |
|---|---|
| 🧩 **عام** | 📊 مركز التحكم • ⚙️ الإعدادات العامة |
| 👥 **الأعضاء** | 👋 الترحيب والوداع • 🎭 الرتب التلقائية • 📈 المستويات • ➖ الخط الفاصل • 😀 التفاعلات |
| 🛡️ **الحماية** | 🛡️ الحماية التلقائية • ⚖️ عقوبات التحذيرات |
| 🎫 **التذاكر** | 🎫 نظام التذاكر • 📝 تقديم الإدارة |
| 📜 **السجلات** | 📜 سجلات السيرفر (38 حدثًا) |
| 🗂️ **البيانات** | 🗂️ الحالات والتذاكر والمتصدّرون |

**ما يجعلها سهلة الاستعمال:**
- 🔍 **بحث في الإعدادات**: اكتب «تذاكر» أو «ترحيب» ويصفّي القائمة فورًا.
- ⚡ **مركز التحكم**: 11 مفتاح لتشغيل/إطفاء الأنظمة بضغطة، ورسم بياني لنشاط 14 يومًا.
- 📝 **مرحبا** كل الحقول واضحة: نص مقابل نص، مفتاح مقابل تشغيل/إطفاء، وقائمة منسدلة مقابل اختيار قناة/رتبة.
- ➕ **محرّرات قوائم** (قنوات، رتب، كلمات ممنوعة، إيموجيات): أضف بشريحة وحذف بزر ✕.
- 💾 **شريط حفظ واحد** أسفل الشاشة يظهر فقط لمّا يكون في تغيير، مع زر «↩️ تراجع».
- 🟢 **نقاط ملوّنة** بجانب كل قسم: أخضر = النظام مفعّل، رمادي = معطّل.
- 🧪 **أزرار تجريبية**: إرسال رسالة ترحيب (مع البطاقة)، سجل تجريبي، خط فاصل، أو نشر لوحة التذاكر — مباشرة من اللوحة.
- 📱 **تصميم متوافق مع الجوال** والاتجاه من اليمين لليسار.

> 🎛️ كل إعداد في اللوحة له أمر سلاش مكافئ، والعكس صحيح.

### 🌗 وضعان: ليلي ونهاري
زر 🌙/☀️ في أعلى كل صفحة يبدّل الوضع فورًا، ويُحفظ الاختيار في المتصفح (`localStorage`) بدون وميض عند إعادة التحميل.

### 🔐 تأكيد الدخول + الرول المطلوب
الموقع **ما يفتحه إلا من سجّل دخوله بحساب Discord** (`LOGIN_REQUIRED=true`)، وبعد الدخول لازم يكون عنده الرول المطلوب.
- **الخطوة ١ — تأكيد الدخول:** أي زائر يُحوَّل إلى `/auth/login` (تسجيل Discord)، ثم يرجع لنفس الصفحة التي طلبها.
- **الخطوة ٢ — الرول:** يُفحص الرول `REQUIRED_ROLE_ID` (افتراضيًا `1549364852433354792`) عند العضو داخل سيرفرات البوت.
  - يملكه → يفتح الموقع كامل (قراءة + تعديل).
  - ما يملكه → صفحة **«الوصول مقيّد»** تشرح الخطوات (ادخل السيرفر • اطلب الرول • أعد الفحص) مع أزرار إعادة الفحص والدخول بحساب آخر.
  - البوت متوقف → رسالة واضحة أن التحقق متعذّر حتى يعود البوت.
- **على مستوى الـAPI:** قراءة/كتابة بدون رول → `403 role_required` برسالة عربية. بدون تسجيل → `401`.
- **الكاش:** نتيجة الفحص تُحفظ في الجلسة 5 دقائق (`ROLE_CACHE_SECONDS`) لتقليل الطلبات على ديسكورد.
- **التحكم:** `REQUIRED_ROLE_ID=` فارغًا يعني قبول أي عضو مسجّل.

### 🔄 المزامنة الحقيقية — معلومات سيرفرك في الموقع
الموقع يزامن معلومات سيرفرك الفعلية من ديسكورد ويحفظها، فلا يعرض بيانات وهمية:

| ما يُزامَن | التفاصيل |
|---|---|
| السيرفر | الاسم • الأيقونة • عدد الأعضاء • المالك • تاريخ الإنشاء • مستوى البوستات |
| القنوات | كل القنوات النصية والصوتية والأقسام (تُستخدم في كل القوائم المنسدلة باللوحة) |
| الرتب | الأسماء والألوان والترتيب (تُستخدم في الرتب التلقائية والمكافآت والصلاحيات) |
| الإيموجيات | إيموجيات السيرفر لعناصر التفاعل التلقائي |

**كيف تشتغل:**
- عند تشغيل البوت: مزامنة فورية، ثم **كل 5 دقائق** (`SYNC_INTERVAL_SECONDS`)، وأيضًا فور أي تغيير (سيرفر/قناة/رتبة) — واللوحة تحدّث نفسها **لحظيًا** عبر بث حيّ (SSE) بدون تحديث الصفحة.
- البوت متوقّف؟ المزامنة تعمل عبر واجهة ديسكورد بالتوكن، وإن تعذّر ذلك يعرض الموقع **آخر لقطة محفوظة** مع وقتها («آخر مزامنة: قبل …»).
- زر **«مزامنة الآن»** موجود في قائمة السيرفرات وفي كل صفحة إعداد + حالة المزامنة ظاهرة في `/api/sync/status`.
- بمجرد وضع `DISCORD_TOKEN` حقيقي في `.env` تتوقّف بيانات العرض تلقائيًا (وتُحذف) وتصير الصفحات تعرض سيرفراتك الحقيقية فقط.
- كل حفظ من الموقع يطبّقه البوت مباشرة (نفس قاعدة البيانات) — ترحيب، حماية، سجلات، تذاكر، مستويات… بدون أي خطوة إضافية.

### 🔗 أمر /help يفتح الموقع فعلًا
عند إرسال `/help` يظهر زر **«افتح الموقع»** وزر **«لوحة التحكم»** — روابط مباشرة تفتح الموقع في المتصفح (زر Link في ديسكورد)، وتظهر كذلك في تفاصيل أي أمر (`/help الأمر:ban`) وفي التذييل.
الرابط يُحدَّد تلقائيًا بالترتيب:

1. `DASHBOARD_URL` إن كان مضبوطًا في `.env` (اختيارك أنت).
2. نطاق منصات النشر: Railway (`RAILWAY_PUBLIC_DOMAIN`) • Render • Fly • Vercel • `PUBLIC_URL`.
3. رابط بيئة المعاينة الحالي، وإن كان `DASHBOARD_URL` رابط معاينة قديمًا من جلسة سابقة يُصحَّح تلقائيًا (روابط المعاينة تتغيّر كل جلسة).
4. `http://localhost:3000` كحل أخير.

**مهم:** رابط `localhost` يفتح فقط على الجهاز الذي يشغّل المشروع، ولازم يكون المشروع شغّالًا وقت الضغط على الزر. إذا كان الرابط محليًا يظهر في `/help` تنبيه بذلك بدل كلام مضلّل.
وللتأكد بسرعة:

```bash
npm run check:site     # يفحص الرابط المحلي والعام ويقول لك سبب المشكلة بالعربي
```

### 🚫 بدون ميزة «إضافة البوت»
الموقع لا يحتوي أي زر أو رابط لدعوة البوت (`inviteUrl` أُزيل من الإعدادات والزر من `/help` والرئيسية). دعوة البوت تصير من Developer Portal بصلاحياتك أنت — راجع قسم «إعداد ديسكورد».

### 🌍 الوصول العام (اختياري — معطّل افتراضيًا الآن)
الموقع مفتوح للجميع للقراءة: أي زائر يفتح الصفحة الرئيسية، ويشاهد قائمة السيرفرات، ويتصفّح إعدادات أي سيرفر مُدرَج فيه البوت — **بدون تسجيل دخول**.
- **القراءة:** متاحة للجميع (GET) عند `PUBLIC_ACCESS=true` (الافتراضي).
- **التعديل:** يحتاج تسجيل دخول بحساب Discord — نقاط الكتابة في الـAPI محميّة بـ401، وتظهر للزائر شارة «عرض عام» مع زر تسجيل الدخول بدل شريط الحفظ.
- لإغلاق الموقع تمامًا: `PUBLIC_ACCESS=false` → ما يفتحه إلا المسجّلون.

### 📱 يتكيّف مع أي شاشة ومع الجهاز نفسه
- **نقاط تكيّف متعدّدة:** جوال صغير (≤400px) • جوال (≤620px) • تابلت (≤1024px) • شاشة كبيرة (≥1500px) + وضع الجوال الأفقي + أنماط الطباعة.
- **الشريط الجانبي في اللوحة** يتحوّل إلى شريط أفقي قابل للسحب على الجوال، والبطاقات تصير عمودًا واحدًا، والجداول تُسحب أفقيًا.
- **الجهاز نفسه:** السمة تتبع إعداد الجهاز (ليلي/نهاري) تلقائيًا إن لم تختر سمة، والزر صار ثلاث حالات: حسب الجهاز ← نهاري ← ليلي.
- **تفاصيل الجوال:** `viewport-fit=cover` + احترام الحواف الآمنة (نتشة آيفون)، وخط الإدخال 16px لمنع تكبير iOS، وأهداف لمس أكبر (40px+)، وإلغاء تأثيرات التحويم على شاشات اللمس، واحترام «تقليل الحركة».
- **قابل للتثبيت كتطبيق:** أيقونة + مانيفست + وسوم آيفون (`apple-mobile-web-app-capable`).

### 🎨 واجهة رسمية بأيقونات SVG
واجهة الموقع لا تستخدم إيموجيات الكيبورد: كل رمز فيها أيقونة SVG مرسومة داخل الصفحة من مكتبة محلية بدون أي اتصال خارجي — `src/web/public/icons.js` (81 أيقونة + شعار مرسوم + خريطة أيقونات لأحداث السجلات). اللوحة تعرض 38 حدث سجل بـ 38 أيقونة مختلفة بدل الإيموجي.

### 🏠 صفحات الموقع
| الصفحة | الوصف |
|---|---|
| `/` | صفحة رئيسية رسمية: مزايا الأنظمة، خطوات البدء، أمثلة أوامر، وزرّا «فتح اللوحة» و«حالة الخدمة» |
| `/dashboard` | قائمة سيرفراتك: بحث بالاسم + فلترة (الكل / البوت موجود / بدون البوت) + زر إعداد لكل سيرفر |
| `/dashboard/:id` | لوحة إعداد السيرفر (13 قسمًا) |
| `/api/status` | حالة البوت (JSON) |

## 🔓 تشغيل الموقع بدون وضع المعاينة (حقيقي 100%)
الموقع يعمل الآن ببيانات سيرفر افتراضية ليُعرض بدون توكن. لتشغيله بحساب Discord حقيقي:

1. ضع `DISCORD_TOKEN` و`CLIENT_ID` و`CLIENT_SECRET` في `.env`.
2. اجعل `DEMO_MODE=false`.
3. في Developer Portal → OAuth2 → Redirects أضف: `https://نطاقك/auth/callback`.
4. أعد التشغيل: `npm start` — وسيصبح الدخول بتسجيل Discord حقيقي، وتظهر سيرفراتك الحقيقية فقط.

> ملاحظة: قيمة `DASHBOARD_URL` تُستخدم في روابط العودة وفي الـ sitemap، فتأكد أنها نطاقك الحقيقي.

## ⚙️ الإعدادات الكاملة

ملف `.env`:

```env
DISCORD_TOKEN=توكن_البوت
CLIENT_ID=ايدي_التطبيق
CLIENT_SECRET=سر_التطبيق

DASHBOARD_URL=http://localhost:3000
DASHBOARD_PORT=3000
SESSION_SECRET=نص_طويل_عشوائي
DEMO_MODE=false              # true = معاينة اللوحة بدون بوت

DEFAULT_LANGUAGE=ar          # ar | en
DEVELOPER_IDS=               # أيدي المطوّرين (مفصولة بفاصلة) — محصّنون من العقوبات
DATABASE_DRIVER=sqlite       # sqlite | json
DATABASE_PATH=./data/neverland.db
RESET_COMMANDS=false         # true = إعادة تسجيل الأوامر عند كل تشغيل
```

القيم الافتراضية لكل سيرفر موجودة في `src/config.js` داخل `defaults`
(ترحيب، وداع، لوقات، حماية، مستويات، تذاكر، رتب تلقائية...) ويمكن تعديلها من اللوحة.

---

## 📁 هيكل المشروع

```
never-land/
├── src/
│   ├── index.js               ← نقطة التشغيل (بوت + لوحة)
│   ├── client.js              ← عميل ديسكورد والـ Intents
│   ├── config.js              ← الإعدادات والقيم الافتراضية
│   ├── deploy-commands.js     ← تسجيل أوامر السلاش
│   ├── commands/              ← الأوامر (25 أمر)
│   │   ├── general/  moderation/  config/  tickets/  utility/
│   ├── events/                ← الأحداث (24 حدثًا)
│   ├── systems/               ← الأنظمة الكبرى
│   │   ├── automod.js  logging.js  welcome.js  leveling.js
│   │   ├── tickets.js  setupWizard.js  autoline.js  autoreact.js
│   │   ├── applications.js
│   ├── lib/                   ← أدوات مشتركة
│   │   ├── i18n.js  embeds.js  utils.js  permissions.js
│   │   ├── modActions.js  audit.js  levels.js  emojis.js  welcomeCard.js
│   ├── database/              ← طبقة البيانات
│   │   ├── index.js  sqlite.js  json.js  defaults.js  schema.sql
│   ├── handlers/              ← محمّلات الأوامر والأحداث
│   └── web/                   ← لوحة التحكم
│       ├── server.js  demo.js
│       ├── routes/  (auth, api, pages)
│       └── public/  (style.css, app.js)
├── tests/smoke.js             ← اختبارات سريعة
├── data/                      ← قاعدة البيانات (تُنشأ تلقائيًا)
├── package.json   .env.example   Dockerfile   docker-compose.yml   ecosystem.config.js
```

**إضافة أمر جديد** = أنشئ ملفًا في `src/commands/<قسم>/` يُصدّر `{ data, run }` فقط —
يُحمَّل تلقائيًا ويُسجَّل في ديسكورد عند التشغيل.

---

## 🗄️ قاعدة البيانات

| الجدول | المحتوى |
|---|---|
| `guilds` | إعدادات كل سيرفر (JSON مرن — أضف أي ميزة بدون تعديل الجداول) |
| `cases` | كل العقوبات (تحذير، حظر، طرد، إسكات...) |
| `tickets` | التذاكر وحالتها وأرشيفها |
| `levels` | الخبرة والمستويات والرسائل والدقائق الصوتية |
| `stats_daily` | إحصائيات يومية (للرسوم البيانية) |
| `reminders` | التذكيرات المجدولة |

- **الافتراضي: SQLite** عبر `better-sqlite3` (سريع جدًا ومحلي).
- **بديل بدون أي مكتبة أصلية:** `DATABASE_DRIVER=json` — مفيد إذا فشل تثبيت `better-sqlite3`.

---

## 🌍 نشْر الموقع — رابط يفتح من أي جهاز (جوال/كمبيوتر)

`localhost` يفتح فقط على الجهاز الذي يشغّل المشروع. لرابط يعمل من أي مكان:

### أ) رابط فوري مؤقت (بلا حساب، ثوانٍ)
```bash
npm run web                 # نافذة أولى: الموقع
npm run link:public         # نافذة ثانية: يطبع رابط https عام
# أو احفظه تلقائيًا في .env ليستخدمه /help:
npm run link:public -- --save
```
- يطبع مثلًا: `https://xxxx-yyyy.trycloudflare.com` — افتحه من الجوال مباشرة.
- **مؤقت**: يعيش ما دام الأمر شغّالًا، ويتغيّر في كل تشغيل.

### ب) رابط دائم (Railway — موصى به)
```bash
npm run deploy:railway      # يثبّت CLI + يسجّل دخولك + ينشئ مشروعًا ويرفعه
```
الملفات جاهزة للنشر مسبقًا: `Dockerfile` • `railway.json` (ويستخدم مسار الصحة **`/healthz`**) • `.dockerignore` • وتشغيل مرن بمتغيّرات `RUN_BOT` / `RUN_WEB`.
في لوحة Railway أضف:
1. **Volume** واربطه بـ `/data` ثم ضع `DATABASE_PATH=/data/neverland.db` — لتبقى بياناتك محفوظة.
2. **Variables**: `DISCORD_TOKEN` • `CLIENT_ID` • `CLIENT_SECRET` • `DEMO_MODE=false` • `RUN_BOT=true` (وبدونها الموقع يشتغل عادي، البوت فقط لا يعمل).
3. **Domain**: أنشئ نطاقًا عامًا، واجعله نفسه في `DASHBOARD_URL` وفي Developer Portal → OAuth2 → Redirects:
   `https://نطاقك/auth/callback`

> للتحقق بعد النشر: `https://نطاقك/healthz` يرجّع `{"ok":true,...}` • و`npm run check:site` يفحص كل شيء محليًا.

## 🚢 النشر على سيرفر

### خيار 1: PM2 (الأسهل)
```bash
npm install -g pm2
pm2 start ecosystem.config.js
pm2 save && pm2 startup
pm2 logs never-land
```

### خيار 2: Docker
```bash
docker compose up -d --build
docker compose logs -f
```

### خيار 3: VPS عادي
```bash
git clone <repo> && cd never-land
npm install --omit=dev
cp .env.example .env && nano .env
npm start
```
> 💡 عند النشر: اجعل `DASHBOARD_URL` هو النطاق العام، وأضف نفس الرابط في
> Discord Developer Portal → OAuth2 → Redirects، ويمكن ربط اللوحة بـ Nginx/Caddy مع HTTPS.

---

## 🔍 استكشاف الأخطاء

| المشكلة | الحل |
|---|---|
| `Used disallowed intents` | فعّل **SERVER MEMBERS** و **MESSAGE CONTENT** في Developer Portal |
| أوامر السلاش لا تظهر | شغّل `npm run deploy` (الظهور فوري في السيرفرات) أو `npm run deploy -- --global` (يستغرق حتى ساعة) |
| البوت لا يعاقب الأعضاء | ارفع رتبة البوت أعلى من رتب الأعضاء + فعّل صلاحياته |
| `🛡️ الحماية لا تعمل` | تأكد أن `/automod status` مُفعّل وأن القناة ليست في قائمة الاستثناء |
| لوحة التحكم لا تسجّل الدخول | طابق `DASHBOARD_URL` مع Redirect URL، وتأكد من `CLIENT_SECRET` |
| `better-sqlite3` فشل في التثبيت | استخدم `DATABASE_DRIVER=json` أو ثبّت أدوات البناء (`build-essential python3`) |
| لا تظهر السيرفرات في اللوحة | تأكد أنك تملك صلاحية **إدارة السيرفر** وأن البوت مُضاف |
| زر «افتح الموقع» في `/help` يطلع `ERR_CONNECTION_REFUSED` | الموقع مو شغّال أو الرابط محلي على جهاز غير جهازك — شغّل `npm start` وشغّل `npm run check:site`، أو انشر الموقع وضع رابطه في `DASHBOARD_URL` |
| رابط الموقع توقّف بعد فترة (بيئة معاينة) | روابط المعاينة تتغيّر كل جلسة — البرنامج يكتشف الجديد تلقائيًا، وللاستقرار انشر على Railway وضع `DASHBOARD_URL` ثابتًا |
| اللوحة تعرض «لم تتم المزامنة بعد» | ضع `DISCORD_TOKEN` صالحًا في `.env` ثم شغّل `npm start`، أو اضغط «مزامنة الآن» بعد الدخول |
| الموقع يعرض «مجتمع Never Land» | هذه بيانات العرض لأن التوكن غير مضبوط بعد — بمجرد إضافة التوكن تُستبدل بسيرفراتك الحقيقية |
| الموقع يفتح عندي بس ما يفتح عند غيري | `localhost` محلي — انشر الموقع (`npm run deploy:railway`) أو افتح نفقًا (`npm run link:public`) ثم اضبط `DASHBOARD_URL` |

---

## 📊 الميزات الكاملة

| الميزة | الحالة |
|---|---|
| الحماية التلقائية + Anti-Raid | ✅ |
| أوامر الإشراف + سجل حالات | ✅ |
| السجلات (38 حدثًا في 9 مجموعات) | ✅ |
| الترحيب والوداع والرتب التلقائية | ✅ |
| نظام التذاكر (4 أنواع + أزرار) | ✅ |
| الخط الفاصل التلقائي (AutoLine) | ✅ |
| التفاعلات التلقائية (AutoReaction) | ✅ |
| صورة ترحيب بالأفتار | ✅ |
| نظام المستويات والرتب | ✅ |
| لوحة تحكم ويب | ✅ |
| دعم العربية الكامل في الردود | ✅ |
| فلتر كلمات يفهم العربية | ✅ |
| مفتوح المصدر وقابل للتعديل | ✅ |
| يعمل محليًا أو على سيرفرك الخاص | ✅ |

---|---|---|
| الحماية التلقائية + Anti-Raid | ✅ | ✅ |
| أوامر الإشراف + سجل حالات | ✅ | ✅ |
| السجلات (18 حدثًا) | ✅ | ✅ |
| الترحيب والوداع والرتب التلقائية | ✅ (بعضها Premium) | ✅ مجانًا |
| نظام التذاكر (4 أنواع + أزرار) | 💎 Premium | ✅ مجانًا |
| الخط الفاصل التلقائي (AutoLine) | ✅ | ✅ |
| التفاعلات التلقائية | ✅ | ✅ |
| صورة ترحيب بالأفتار | 💎 Premium | ✅ مجانًا |
| عدد أحداث السجلات | 30+ | ✅ 38 |
| نظام المستويات | 💎 Premium | ✅ مجانًا |
| لوحة تحكم ويب | ✅ | ✅ |
| **دعم العربية الكامل في الردود** | جزئي | ✅ **كامل** |
| **فلتر كلمات يفهم العربية** | ❌ | ✅ |
| **مفتوح المصدر وقابل للتعديل** | ❌ | ✅ |
| **يعمل محليًا/على سيرفرك الخاص** | ❌ | ✅ |

---

## 📄 الترخيص

MIT — استخدمه، عدّله، وانشره بحرية. بُني بـ [discord.js](https://discord.js.org) v14.
